# Password you set for the Actual Budget server (the server login password).
ACTUAL_SERVER_PASSWORD=changeme-actual-server-password
# A strong random key you invent; callers (n8n) must send it as the x-api-key header.
# This is NOT an Actual credential — it only guards this HTTP wrapper.
API_KEY=changeme-generate-a-long-random-string
